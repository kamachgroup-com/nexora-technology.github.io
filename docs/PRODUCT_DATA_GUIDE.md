# Product Data Guide

Products are stored in `data/products.json`.

Required fields: `id`, `name`, `category`, `price`, `image`, `short`, `specs`, `description`.

For production, add verified SKU, brand, model, inventory, shipping weight, warranty, tax class, compatible accessories and structured product metadata.
